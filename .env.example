# TickTick OAuth (create app at TickTick developer portal)
TICKTICK_CLIENT_ID=
TICKTICK_CLIENT_SECRET=
TICKTICK_REDIRECT_URI=

# Webhook verification (HMAC-SHA256 secret from TickTick webhook registration)
TICKTICK_WEBHOOK_SECRET=

# Database (Vercel Postgres: use POSTGRES_URL from Vercel dashboard, or DATABASE_URL)
POSTGRES_URL=
DATABASE_URL=

# Microsoft Graph OAuth (register app at https://portal.azure.com → App registrations)
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
MICROSOFT_REDIRECT_URI=
# Tenant: "organizations" (work/school), "common" (any), or your tenant ID
MICROSOFT_TENANT=organizations

# Vercel Cron secret (set in Vercel dashboard; Vercel sends it as Authorization: Bearer <CRON_SECRET>)
# To change poll cadence: edit "schedule" in vercel.json
#   Every 1 min (testing): "* * * * *"
#   Every 5 min (production): "*/5 * * * *"
CRON_SECRET=

# Optional: session/auth signing (e.g. for future auth)
NEXTAUTH_SECRET=
